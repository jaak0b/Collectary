<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:installLocation="auto">
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.RECORD_AUDIO" />
	<application android:label="Collectary" android:icon="@drawable/Icon">
		<!--
			Catches the OneDrive MSAL sign-in redirect (msauth://com.collectary.app/<signature-hash>)
			and returns control to the app from the Chrome Custom Tab. The android:path is the raw
			(URL-decoded) signature hash with a leading slash and MUST match the hash registered for
			the Android platform in the Azure app registration. Replace the placeholder with the real
			signing-certificate hash before shipping.
		-->
		<activity android:name="microsoft.identity.client.BrowserTabActivity" android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.VIEW" />
				<category android:name="android.intent.category.DEFAULT" />
				<category android:name="android.intent.category.BROWSABLE" />
				<data android:scheme="msauth"
				      android:host="com.collectary.app"
				      android:path="/&lt;ANDROID_SIGNATURE_HASH&gt;" />
			</intent-filter>
		</activity>
	</application>
</manifest>
